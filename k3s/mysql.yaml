apiVersion: v1
kind: Service
metadata:
  name: traffic-usage-monitor-mysql
  namespace: traffic-usage-monitor
  labels:
    app.kubernetes.io/name: traffic-usage-monitor-mysql
spec:
  selector:
    app.kubernetes.io/name: traffic-usage-monitor-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: traffic-usage-monitor-mysql
  namespace: traffic-usage-monitor
  labels:
    app.kubernetes.io/name: traffic-usage-monitor-mysql
spec:
  serviceName: traffic-usage-monitor-mysql
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: traffic-usage-monitor-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: traffic-usage-monitor-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          envFrom:
            - secretRef:
                name: traffic-usage-monitor-mysql-secret
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - mysqladmin ping -h 127.0.0.1 -uroot -p"$MYSQL_ROOT_PASSWORD"
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - mysqladmin ping -h 127.0.0.1 -uroot -p"$MYSQL_ROOT_PASSWORD"
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
  volumeClaimTemplates:
    - metadata:
        name: mysql-data
        labels:
          app.kubernetes.io/name: traffic-usage-monitor-mysql
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
