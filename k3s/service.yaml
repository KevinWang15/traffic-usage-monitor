apiVersion: v1
kind: Service
metadata:
  name: traffic-usage-monitor-service
  namespace: traffic-usage-monitor
  labels:
    app.kubernetes.io/name: traffic-usage-monitor
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: traffic-usage-monitor
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 25335
      protocol: TCP
